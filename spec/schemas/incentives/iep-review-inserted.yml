name: incentives.iep-review.inserted
title: Incentives - an incentive review was added
contentType: application/json
payload:
  type: object
  properties:
    version:
      const: "1.0"
    eventType:
      const: incentives.iep-review.inserted
    occurredAt:
      type: string
      format: date-time
      example: 2023-12-05T12:34:56+00:00
    description:
      type: string
      example: "An IEP review has been added"
    additionalInformation:
      type: object
      properties:
        id:
          description: Incentive review ID
          type: integer
          example: 1234567
        nomsNumber:
          description: Prisoner number
          type: string
          example: "A1111AA"
        incentiveLevel:
          description: Incentive level code the prisoner holds after this review
          type: string
          example: "STD"
        previousIncentiveLevel:
          description: Incentive level code the prisoner held before this review, absent if this is their first review
          type: string
          example: "BAS"
        incentiveLevelChanged:
          description: Whether this review changed the prisoner's incentive level, absent if this is their first review
          type: boolean
          example: true
